# Mohan Portfolio — React + Vite

This repository contains a migration of the original static HTML/CSS portfolio into a React app scaffolded for Vite.

Quick start:

```bash
cd Portfolio
npm install
npm run dev
```

Dev server runs at http://localhost:5173 by default.
